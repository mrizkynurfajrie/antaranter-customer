import 'package:flutter/material.dart';

class PageTermNebeng extends StatelessWidget {
  const PageTermNebeng({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Halaman Syarat dan ketentuan"),
      ),
    );
  }
}
