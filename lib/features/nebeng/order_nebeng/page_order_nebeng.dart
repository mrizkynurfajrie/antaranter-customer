import 'package:flutter/material.dart';

class PageOrderNebeng extends StatelessWidget {
  const PageOrderNebeng({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("HALAMAN DETAIL ORDERED NEBENG"),),
    );
  }
}